begin
  def divide(x, y, z);
end
const a:=25, b:=7;
var q,r;
def divide(x, y, z);
begin
  x:=0;
  while (y>=z) do
  begin
    y:=y-z; x:=x+1; 
  end;
  return x;
end;


r:=a; 
write call divide(q, r, b)
