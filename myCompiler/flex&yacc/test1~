begin
    def testadd(x, y);
end
const a:=10, b:=20;
var c, d, e;
def testadd(x, y);
    begin
        const A:=99, b:=100;
        var c;
        c:=x+y;        
        write c;
        return c;
    end
c:=a;
d:=b;
e:=call testadd(c, d);
write c;
write d;
write e
