begin
    def gcd(a, b);
end
const a:=84,b:=36;
var x,y,z,q,r;
def gcd(a, b);
  begin
    var ans;
    if b==0 then ans:=a;
    else ans:=call gcd(b, a%b); end
    return ans;
  end

x:=a; 
y:=b; 
z:=call gcd(x, y);
write z





