begin
  def factorial(a);
end
var m,fact;
def factorial(a);
  begin
    if a>0 then
      fact:=fact*a;
      call factorial(a-1);
    end;
    return fact;
  end

m:=6;
fact:=1;
call factorial(m);
write fact
